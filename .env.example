# Copy to .env and edit before running start.sh:
#   cp .env.example .env

DOMAIN=aranyavihaara.org
EMAIL=you@example.com

# 1 = Let's Encrypt staging (untrusted browser cert, but no rate limit — use
#     this while testing the deploy itself). 0 = real certificate.
STAGING=0
